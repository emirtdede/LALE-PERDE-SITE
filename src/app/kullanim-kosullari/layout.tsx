import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • Kullanım Koşulları',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
