import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • Gizlilik Politikası',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
