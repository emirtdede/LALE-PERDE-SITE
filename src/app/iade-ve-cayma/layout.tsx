import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • İade ve Cayma Şartları',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
