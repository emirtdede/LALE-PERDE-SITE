import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • Ön Bilgilendirme Formu',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
