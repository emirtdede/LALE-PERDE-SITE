import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • Çerez Politikası',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
