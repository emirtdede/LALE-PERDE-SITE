import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • KVKK Aydınlatma Metni',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
